[Unit]
Description=RFM amoCRM Webhook Server
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/rfm-amocrm
EnvironmentFile=/opt/rfm-amocrm/.env
ExecStart=/usr/bin/npx tsx server.ts
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
